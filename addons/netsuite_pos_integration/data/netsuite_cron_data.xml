<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ============================================ -->
        <!-- CRON JOB 1: HOURLY ITEM SYNC                -->
        <!-- Syncs all products/items to NetSuite        -->
        <!-- ============================================ -->
        <record id="cron_hourly_item_sync" model="ir.cron">
            <field name="name">NetSuite: Hourly Item Sync</field>
            <field name="model_id" ref="product.model_product_product"/>
            <field name="state">code</field>
            <field name="code">
# Sync all products/items to NetSuite hourly
config = env['netsuite.config'].get_active_config()
if config:
    env['netsuite.api.client'].sync_items_hourly()
            </field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
        </record>

        <!-- ============================================ -->
        <!-- CRON JOB 2: END OF DAY INVOICE SYNC         -->
        <!-- Syncs ALL invoices from today in ONE batch  -->
        <!-- Runs at 11:59 PM daily                      -->
        <!-- ============================================ -->
        <record id="cron_end_of_day_invoice_sync" model="ir.cron">
            <field name="name">NetSuite: End of Day Invoice Sync</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="state">code</field>
            <field name="code">
# Sync all finalized invoices from today in ONE batch
config = env['netsuite.config'].get_active_config()
if config:
    env['netsuite.api.client'].sync_end_of_day_invoices()
            </field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=23, minute=59, second=0)"/>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
        </record>

    </data>
</odoo>

