<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Cron: Process Batch Queue -->
    <record id="cron_process_batch_queue" model="ir.cron">
        <field name="name">NetSuite: Process Batch Queue</field>
        <field name="model_id" ref="model_netsuite_sync_queue"/>
        <field name="state">code</field>
        <field name="code">model.cron_process_batch_queue()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
    </record>

    <!-- Cron: Retry Failed Items -->
    <record id="cron_retry_failed_items" model="ir.cron">
        <field name="name">NetSuite: Retry Failed Syncs</field>
        <field name="model_id" ref="model_netsuite_sync_queue"/>
        <field name="state">code</field>
        <field name="code">model.cron_retry_failed_items()</field>
        <field name="interval_number">30</field>
        <field name="interval_type">minutes</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
    </record>

</odoo>
